(function ($, Drupal, drupalSettings) {

    Drupal.behaviors.kashing_codes  = {
        attach: function (context, settings) {
            //$('#edit-name') DO some stuff

            //console.log(drupalSettings.kashing.kashing_button.foo);
        }
    };

})(jQuery, Drupal, drupalSettings);